{
	"name": "mf-app",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"start": "npm-run-all --parallel serve:auth serve:main serve:resources ",
		"serve:auth": "cd auth && webpack serve",
		"serve:main": "cd main &&  webpack serve",
		"serve:resources": "cd resources &&  webpack serve"

	},
	"devDependencies": {
		"npm-run-all": "^4.1.5",
		"webpack-cli": "^4.9.2"
	}
}
