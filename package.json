{
	"name": "mf-app",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"start": "npm-run-all --parallel serve:auth serve:main serve:student-resources ",
		"serve:auth": "cd auth && webpack serve",
		"serve:main": "cd main &&  webpack serve",
		"serve:student-resources": "cd student-resources &&  webpack serve"
	
	},
  "devDependencies": {
    "webpack-cli": "^4.9.2"
  }
}
